Todo discussion Hygie ( 17 oct )
checker si l'ordre des config a une importance ( si on commence par SO, WE ... )
faire aussi un gestionnaire pour les map de taille trop grandes ou trop petites

ATTENTION ( 16 oct )
Pour invalid color le mess qui suit ne s'affiche pas
Pas le temps de chercher pourquoi, a verif plus tard




UPDATE TO DO PARSING 

faire des maps simples
map complexes
map fausses pour tester validité ?


faire le parsing du projet

check extension .cub
check presence de toutes les textures NO, SO, WE, EA
check les couleurs ( entre 0 - 255)
check validité des maps
entourée de murs (1)
validité des caracteres comme ds so_long mais avec 0, 1, N, S, E, W
faire des messages d’erreur pour chaque cas et quitter de maniere clean
creer des structures

- The map must be composed of only 6 possible characters: 0 for an empty space,
1 for a wall, and N,S,E or W for the player’s start position and spawning
orientation.
- The map must be closed/surrounded by walls
- Except for the map content, each type of element can be separated by one or more empty line(s).
- Except for the map content which always has to be the last, each type of element can be set in any order in the file.
- Except for the map, each type of information from an element can be separated
by one or more space(s).
- The map must be parsed as it looks in the file. Spaces are a valid part of the
map and are up to you to handle. You must be able to parse any kind of map,
as long as it respects the rules of the map.

we are scanning the map from top to bottom, left to right :

    Ignore all leading whitespaces.
    If the current row is the 0th row or the final row, only accept '1's and ' 's.
        else, The first and final character should always be a '1'.
    In the case of any non leading whitespaces, the only acceptable characters adjacent to the space are '1's or ' 's.
    If strlen(curr_row) > strlen(row_on_top) && current col > strlen(row_on_top), current character should be '1'
    If strlen(curr_row) > strlen(row_on_bottom) && current col > strlen(row_on_bottom), current character should be '1'


Conseils de Benedicte et choses auxquelles penser aussi :  

toute la configuration doit etre avant la map. Rien ne doit etre apres sinon invalide.  
Donc prendre ca en compte dans les conditions de map invalides.

Floodfill, faire appel recursif.
Parser la map pour chaque "bloc d'element" ( couleur, FC, ...)
Dasn mon so_long j'avais remplace par "*" qd on etait deja passe dessus, donc penser a refaire pareil ? 
Check si ok ... 

Attention si il y a des pieces non communiquantes, decide si on les parse aussi, ou si on considere juste qu'elles sont pas valides et on skip. 
depend si on fait les bonus ? portes ? teleportation ? Penser a demander a hygie mais maybe not ... 
